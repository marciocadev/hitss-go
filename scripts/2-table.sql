CREATE TABLE hitss.cliente(
  id varchar(250) PRIMARY KEY,
  cpf varchar(11),
  nome varchar(50),
  sobrenome varchar(50),
  contato varchar(50),
  endereco varchar(250),
  dtNascimento varchar(10)
);
